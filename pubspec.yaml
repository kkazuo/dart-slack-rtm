name: slack_rtm
description: A library for Slack RTM client developers. (https://api.slack.com/rtm)
version: 0.2.3
homepage: https://github.com/kkazuo/dart-slack-rtm

environment:
  sdk: '>=1.20.1 <3.0.0'

dependencies:
  http: ^0.13.5

dev_dependencies:
  test: any
